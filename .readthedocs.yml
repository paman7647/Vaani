# Legacy Read the Docs config filename.
# Modern builds use `.readthedocs.yaml`.
# This file is provided for compatibility and mirrors the main config.

version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"

sphinx:
  configuration: docs/conf.py
  fail_on_warning: false

python:
  install:
    - requirements: requirements.txt
